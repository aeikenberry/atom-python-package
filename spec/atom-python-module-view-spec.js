'use babel';

import AtomPythonModuleView from '../lib/atom-python-module-view';

describe('AtomPythonModuleView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
